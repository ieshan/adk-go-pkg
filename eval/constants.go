package eval

// Constants used across the evaluation package.

const (
	// DefaultLiveTimeoutSeconds is the default timeout for live eval sessions.
	DefaultLiveTimeoutSeconds = 300

	// EvalSessionIDPrefix is the prefix for eval session IDs.
	EvalSessionIDPrefix = "___eval___session___"

	// UserAuthor is the author name for user messages in eval events.
	UserAuthor = "user"
)
